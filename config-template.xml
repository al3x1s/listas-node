<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE properties SYSTEM 'http://java.sun.com/dtd/properties.dtd'>
<properties>

    <!-- SERVER CONFIG -->
    <entry key='web.port'>3020</entry>
    <entry key='web.debug'>true</entry>

    <!-- MAIL CONFIG -->
    <entry key='mail.user'>"x.x@hotmail.com"</entry>
    <entry key='mail.password'>x</entry>
   
    <!-- DATABASE CONFIG -->
    <entry key='database.url'>-</entry>
    <entry key='database.dbname'>x</entry>
    <entry key='database.user'>x</entry>
    <entry key='database.password'>x</entry>
    <entry key='database.refreshDelay'>20</entry>

    <!-- QUERIES -->
    <entry key='database.selectPersonal'>
        SELECT * FROM personal;
    </entry>

    <entry key='database.selectVehicles'>
        SELECT * FROM vehiculo;
    </entry>

    <entry key='database.insertLogMail'>
        INSERT INTO lista_logger(almacenadora_id, raw, emailTo) VALUES (?,?,?);
    </entry>

    <entry key='database.getListados'>
        SELECT id, UNIX_TIMESTAMP(fecha) as fecha, almacenadora_id, raw FROM lista_logger LIMIT 30;
    </entry>

    <entry key='database.getDestinatarios'>
        SELECT * FROM destinatario_mail;
    </entry>

</properties>